export interface Partner {
    id: number
    name: string
    partnerType: string
    rating: number
    address: string
    director: string
    phone: string
    email: string
    discount: number
}