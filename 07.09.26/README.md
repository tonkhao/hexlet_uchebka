import_partners_modified:

1. Удалены лишние пробелы: Из названий компаний company_name вырезаны все случайные пробелы в начале и в конце строк.

2. Исправлен телефон в третьей строке: Запись +78125554433 приведена к красивому стандартному виду маски +7 (812) 555-44-33, который используется в первой строке.

3. Сохранены пустые ячейки: Пустой телефон во второй строке и пустой рейтинг в третьей оставлены нетронутыми (база запишет их как NULL), чтобы структура таблицы при импорте не поехала.

import_sales_modified:

1. Файл переименован в .csv

2. Даты приведены к единому стандарту СУБД (YYYY-MM-DD), заменил разделители на стандартные запятые и подготовил чистый массив для импорта

3. Также после самого импорта заменил product_name на product_id в самой таблице sales + сделал product_id FK (решил не оставлять без привязки)

Скриншоты успешно выполненных операций в папке tests

Также обновил ERD и shema.sql и убрал лишние сущности



